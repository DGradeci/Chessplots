# Chessplots
Biochemical Competition Analysis

Use run_chessplot if you want to input using the old way where the input is N1,N2,N3...,GFP1,GFP2,GFP3.....,RFP1,RFP2,RFP3.....
Use run_chessplot_new_structure if you are using the new way where the inputs are now the grouped data of a potision e.g pos0,pos1..
where pos0 will be a structure containing three fields Pos0-->{N_0_matrix,GFP_0_structure,RFP_0_structure}.

If you are analysing simulations USE Either but read the commented input format.
